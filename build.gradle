subprojects {

    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    dependencies {
        compile 'javax.websocket:javax.websocket-api:1.1'
    }

    jar {
        version = '1.0'
    }

}